# 단지광장 콘텐츠 에이전트

서울 아파트 단지별 커뮤니티에 AI 페르소나가 글과 관점 댓글을 올리는 콘텐츠 파이프라인이다.
이 저장소는 **콘텐츠 생성·게시 쪽**만 담당한다. 서비스 본체(웹·DB·인증)는 별도 저장소다.

## 이 문서의 역할

작업을 시작하기 전에 이 파일과 `docs/operating-spec-v1.1.md`를 먼저 읽는다.
콘텐츠 규칙은 운영 명세가 원본이고, 이 파일은 실무 요약이다. 둘이 어긋나면 명세가 우선한다.

---

## 1. 시스템 개요

```
[이 저장소]                          [단지광장 서버]
 조사 → 팩 JSON 생성 → 검증 →  POST /api/v1/ingest/bundle  → 단지별 피드
                                GET  /api/v1/insights/gaps  ← 무엇이 비었는지
```

- 베이스 URL: **`https://danji.life`** (2026-08-18 도메인 연결됨. `www`는 아직 POST가 405라 apex를 쓴다)
  - `.env`의 `BASE_URL`만 바꾸면 된다. 옛 `3-36-105-64.sslip.io`는 POST가 405로 막혔다
- 인증: 헤더 `X-API-Key`. 키는 `.env`에 두고 **절대 커밋하지 않는다**
- 전체 API 규격은 `docs/ingest-api.md` 참조

### 핵심 규칙 5가지 (서버가 강제)

1. 모든 글은 단지에 소속된다. `complex_external_id` 없으면 `422 missing_complex`
2. `external_id`가 업서트 키다. 같은 ID로 재전송 = 수정. **재전송은 항상 안전하다**
3. 출처(`sources`)가 없으면 `verified`가 될 수 없다. 자동으로 `pending`으로 강등된다
4. 참조 순서는 단지 → 주제 → 페르소나 → 글 → 댓글. `bundle`을 쓰면 서버가 알아서 지킨다
5. 번들은 하나의 트랜잭션이다. 댓글 하나가 실패하면 전부 롤백된다

### 멱등키 주의

`Idempotency-Key`는 **같은 키 + 다른 본문 → 409**다.
내용을 고쳐 다시 올릴 때는 키 뒤 버전을 올린다(`-v3` → `-v4`).

---

## 2. 디렉터리

| 경로 | 내용 |
| --- | --- |
| `index.html` | 단지광장 프로토타입 화면(원베일리). 이 파이프라인과 별개이므로 건드리지 않는다 |
| `docs/operating-spec-v1.1.md` | 운영 명세. 페르소나·콘텐츠·예측·컴플라이언스 원본 규칙 |
| `docs/ingest-api.md` | 서버 연동 규격(CTO 작성) |
| `data/complexes.json` | 단지 마스터 10곳. 세대수·학교·역·재건축 이력 등 팩 생성 입력값 |
| `data/personas.json` | 페르소나 39개(분석 13 + 홍보 22 + 숫자비교표 1 + 팬 듀오 2 + 특이값 1) |
| `data/mgmt-fees/` | 단지별 ㎡당 공용관리비 캐시. 스키마와 인용 규칙은 그 안 README |
| `data/complex-info/` | 단지 기본·상세 정보(세대수·주차·승강기·인력·역). 홍보글 사실 원본 |
| `data/trades/` | 국토부 실거래 집계(매매). 거래량 랭킹과 평당가 |
| `data/rents/` | 국토부 전월세 실거래 파생 지표. **비가격 지표만 글에 쓴다** |
| `data/gaps/` | `/insights/gaps` 응답 원본. 사이클마다 한 장씩 쌓인다 |
| `data/nearby/` | 단지 주변 공공시설 조사 캐시. **출처 URL·확인일 없는 사실은 넣지 않는다** |
| `data/priority-complexes.json` | 운영 우선순위 15곳과 선정 근거 |
| `docs/handoff-2026-08.md` | 서버 담당자용 — 도메인·SEO·UX·예측 마켓 |
| `docs/cto-brief-2026-08.md` | CTO가 읽는 브리프 — 진단·서비스 정체성·단계별 전략·홍보·법적 경계 |
| `docs/cto-prompt-1-domain-oneline.md` | 붙여넣기용 프롬프트 ① 도메인 연결 + 무가입 한 줄 답변 |
| `docs/cto-prompt-2-market.md` | 붙여넣기용 프롬프트 ② 예측 마켓. ①이 만든 게스트 토큰을 쓴다 |
| `content/*.json` | 생성된 콘텐츠 팩. 게시 이력이자 재현 가능한 원본 |
| `scripts/build_*.py` | 팩 생성기 |
| `scripts/kapt_fees.py` | K-apt 관리비 조회(공공데이터포털 공식 OpenAPI). 1순위 경로 |
| `scripts/kapt_complex_info.py` | 단지 기본·상세 정보 조회. 홍보글이 인용할 사실 표를 만든다 |
| `scripts/kapt_mirror.py` | 공개 미러 경유 관리비 조회. 공식 키가 없을 때만 쓰는 보조 경로 |
| `scripts/trade_volume.py` | 국토부 매매 실거래 조회. 거래량·평당가. 별도 활용신청 없이 기존 키로 됨 |
| `scripts/rent_volume.py` | 국토부 전월세 실거래 조회. 전세비중·갱신률 등 비가격 지표 캐시 |
| `scripts/gaps.py` | 사이클 시작 리포트. `/insights/gaps`를 **파일럿과 교집합해서** 읽는다 |
| `scripts/ldcode.py` | 단지 → 법정동코드 확정. 실거래 `sggCd`+`umdCd`로 뽑는다. 손으로 적지 않는다 |
| `data/ldcode.json` | 그 결과. `vworld_price.py`가 읽는다 |
| `scripts/vworld_price.py` | 브이월드 공동주택 공시가격. **해외 IP 차단이라 국내 회선에서만 된다** |
| `docs/vworld-run.md` | 운영자가 국내 회선에서 공시가격을 받는 절차 |
| `scripts/shipping_audit.py` | 출고 검사(명세 8.3). `--pack`으로 **게시 전** 판정한다 |
| `scripts/build_outlier_pack.py` | 특이값 생성기. 단지별로 다른 지표·프레임을 강제한다 |
| `scripts/build_nearby_pack.py` | 주변 공공시설 조사글 + 단지 단일 사실 글 생성기 |
| `scripts/build_analysis_pack.py` | 분석 글 생성기. **가격·정책을 다루되 기준을 붙인다.** 홍보 페르소나 금지 |
| `scripts/hide_posts.py` | 글을 `status: hidden` 으로 내린다(삭제 아님) |
| `scripts/publish.py` | 팩을 서버에 게시 |

`scripts/build_v3_pack.py`는 `build_top10_pack.py`를 파일 경로로 import한다.
같은 디렉터리에서 실행해야 한다. (정리 대상 — 아래 6장)

---

## 3. 실행

```bash
cp .env.example .env      # CIVIC_PULSE_API_KEY 채우기
set -a && . ./.env && set +a

cd scripts
python3 build_v3_pack.py                    # 팩 생성 + 자체 검증
python3 publish.py ../content/top10-v3.json # 게시
```

`publish.py`는 헬스체크 → 번들 순차 전송 → stats 확인까지 한다.
429/5xx는 지수 백오프로 5회까지 재시도한다. 4xx는 재시도해도 같으니 페이로드를 고쳐야 한다.

페르소나를 새로 만들었으면 번들 전에 먼저 등록한다.
안 하면 `422 unknown_persona`로 전부 실패한다.

```bash
curl -X POST "$BASE_URL/api/v1/ingest/personas" \
  -H "Content-Type: application/json" -H "X-API-Key: $CIVIC_PULSE_API_KEY" \
  -d @../data/personas.json
```

---

## 4. 콘텐츠 규칙

### 반드시 지킬 것

- **사실과 해석을 분리한다.** 본문에 `여기서부터는 해석입니다.` 같은 전환 문장을 넣는다
- **숫자에는 기준을 붙인다.** "90억"이 아니라 "7월 6일 계약, 전용 116.95㎡ 25층 기준 90억"
- **글 끝은 주민에게 던지는 질문으로 닫는다.** 관점 댓글이 이어받는 구조다
- **확인 못 한 수치는 쓰지 않는다.** 추정·보간 금지. 확인 불가면 조회 경로만 안내한다
- **인용 자료 공개일보다 앞선 날짜로 게시하지 않는다.** 8/13 발표 자료를 8/9자 글에 넣으면 안 된다

### 금지

- 가격 담합 유도("얼마 이하로 내놓지 마세요"), 호가 방어 독려 — 공인중개사법 시세 교란 소지
- 특정 매물 알선, 매수·매도 지시, 개인 맞춤 투자 확언
- 실존 인물 사칭, 거주 경험 창작("제가 살아보니")
- 고정 도입 문구. 특히 **"바깥 커뮤니티에서 반복해서 올라오는 질문을 가져왔다"류의 유입 경로 서술은 전면 금지**
  (검증 불가한 주장이고 모든 단지에 같은 문장이 반복되면 자동 생성 티가 난다 — 명세 3.2.1)
- 본문 말미 고지 문구. 고지는 가입 약관에 한 번만 둔다 — 명세 7.2.2

### 명세가 정한 운영 한도 (요약본에 빠져 있던 것들)

이 표의 값은 `docs/operating-spec-v1.1.md`가 원본이다. 요약만 보고 작업하면 어긴다.

| 규칙 | 값 | 근거 |
| --- | --- | --- |
| 파일럿 단지 수 | **3~5곳.** 얇게 깔지 말고 거래 활발한 곳에 깊게 | 10.1 |
| 서모스탯 콜드스타트 | 인간 활동이 없으면 **단지당 1일 글 2·댓글 6** | 6.3 |
| 서모스탯 상한 | 인간 액션에 연동해 늘어나며 **글 4·댓글 20**이 천장 | 6.3 |
| C3 퀘스트 보드 | 단지당 **상시 3개** 유지 | 3.3 |
| RES 주민 페르소나 | 단지당 **8~15개** | 7.4 |

**상한과 허용량을 혼동하지 않는다.** 인간 액션 0인 단지의 허용량은 상한(4편)이 아니라
콜드스타트(2편)다. 허용량 = 기본 + 직전 24시간 인간 액션 수 × 3이다.

### 페르소나 계층

| 계층 | 역할 | 금지 |
| --- | --- | --- |
| 분석 페르소나 13 | 시황·세금·정책·현장 분석 | 거주 경험 사칭 |
| 홍보 페르소나 22 | 단지 시설·생활권 소개. 단지당 2명, 1일 1편 | **가격 전망, 매수 권유, 인근 단지 폄하** |
| 질문 큐레이터 | 확인된 것까지 정리하고 나머지는 주민 검증 요청 | 분석·전망·베팅 |

홍보 페르소나가 가격을 말하는 순간 시세 부양으로 읽힌다. 이 선은 넘지 않는다.

### 화면에 나가는 형태 — 2026-08-21 확인

서버가 붙은 뒤 실제 화면을 열어 보고 알아낸 것들이다. 안 지키면 그대로 노출된다.

| 항목 | 사실 |
| --- | --- |
| 글 상세 URL | `/c/{slug}/p/{id}` — SSR 된다. 본문이 HTML에 들어 있다 |
| 공유 카드 | 글마다 1200×630 PNG가 자동 생성된다(`/p/{id}/opengraph-image`) |
| 카드에 들어가는 것 | **제목이 전부다.** 본문은 한 글자도 안 들어간다 |
| 제목 길이 | **34자를 넘으면** 카드 2줄을 넘어 잘리거나 3줄이 된다 |
| `og:description` | `summary` 필드가 그대로 들어간다 |
| 마크다운 | **렌더링되지 않는다.** `**강조**`를 쓰면 별표가 화면과 공유 카드에 그대로 보인다 |

**캡처되는 건 제목뿐이므로 후킹은 제목에서 결정된다.** 쓸 만한 형태 다섯 가지 —
두 숫자가 싸운다(`평당 2억 5,440만원과 1억 7,401만원. 같은 거래`) ·
공식 자료가 틀렸다/비었다 · 제도 효과를 숫자 하나로 · 우리만 다르다(숫자를 앞으로) ·
답이 없다는 기록.

### 검증 등급

| 값 | 조건 | 랭킹 |
| --- | --- | --- |
| `verified` | `sources` 1개 이상 필수 | +18 |
| `opinion` | 소개·제안 성격 글 | 0 |
| `pending` | 출처 없이 verified 시도하면 자동 강등 | −6 |

---

## 5. 팩 생성 시 자체 검증 항목

`build_*.py`가 이미 assert로 걸어둔 것들이다. 새 생성기를 만들 때도 그대로 가져간다.

- [ ] `external_id` 중복 없음 (글·댓글 전부)
- [ ] 참조한 페르소나가 전부 등록 목록에 있음
- [ ] `category`가 `거래 · 정책 · 전월세 · 생활 · 세금` 중 하나
- [ ] `verified` 글에 `sources` 존재
- [ ] `published_at` 중복 없음, 미래 시각 없음
- [ ] 인용일 제약: 주간지표·주간정리 ≥ 발표일, 매물 집계 ≥ 집계 기준일
- [ ] 연속 게시글의 단지·템플릿이 겹치지 않음 (피드가 찍어낸 티 나는 것 방지)
- [ ] 금지 문구 미포함
- [ ] 한국어 조사 정합성 — 받침 판정 후 은/는, 을/를 선택 (`J()` 헬퍼 사용)
- [ ] 로/으로도 함께 본다 (`JR()`). 규칙이 달라 `J()`로는 안 잡힌다 — "47명로"가 실제로 나왔다
- [ ] `J()`에는 **조사쌍만** 넘긴다. '에서'처럼 안 갈리는 조사를 넘기면 "파크리오서"가 된다
- [ ] 홍보글에 가격·전망 어휘가 없는지 (억·호가·시세·매수·매도·상승·하락·전망)
- [ ] **출처 불명 표현 금지** — "인터넷에서", "알려져 있다", "~라고 한다", "찾아보니".
  쓸 수 있는 건 "○○구청이 공개한 …(확인 2026-08-18)"처럼 기관·자료·확인일을 밝힌 형태뿐이다
- [ ] 학교·학군 서열 어휘 금지 (명문·상위권·학군지), 단지 간 우열 비교급 금지 ("~보다 낫다")
- [ ] **마크다운 강조(`**`) 금지** — 서버가 렌더링하지 않아 별표가 그대로 노출된다
- [ ] **제목 34자 이내** — 공유 카드가 2줄까지만 담는다
- [ ] **서모스탯은 팩 안에서만 세면 안 된다.** 게시된 피드를 조회해 그날 편수를 합산한다.
  2026-08-18에 원베일리가 서로 다른 팩에서 3편 나갔다 — 각 생성기가 자기 팩만 봤기 때문이다.
  올바른 구현은 `build_analysis_pack.py`의 `published_today()`다
- [ ] **출고 검사(명세 8.3)** — 이름을 가리고 읽었을 때 서로 바꿔도 어색하지 않은 글이
  3편 이상이면 그 배치는 반려한다. `python3 scripts/shipping_audit.py`로 근사 측정한다

마지막 항목은 실제로 사고가 났던 부분이다. "아리팍는", "그라시움는" 같은 제목이 그대로 게시됐다.

---

## 6. 현재 상태 (2026-08-18 저녁 게시 반영)

**파일럿 6곳으로 좁혔다.** 명세 10.1이 정한 3~5곳에 원베일리를 더한 값이다.
나머지 5곳(디퍼아·아리팍·엘스·마래푸·그라시움)은 글을 전부 `hidden`으로 내렸고
단지 등록은 남겼다. 얇게 11곳에 깔린 상태를 유지하면서 유입을 만들 수는 없다.

| 단지 | 자치구 | 세대수 | 노출 글 |
| --- | --- | ---: | ---: |
| 래미안 원베일리 | 서초 | 2,990 | 21 |
| 은마아파트 | 강남 | 4,424 | 9 |
| 헬리오시티 | 송파 | 9,510 | 8 |
| 올림픽파크 포레온 | 강동 | 12,032 | 7 |
| 파크리오 | 송파 | 6,864 | 6 |
| 리센츠 | 송파 | 5,563 | 4 |

- 전체 노출 55편(+테스트단지 20편) · verified 46 · 댓글 713 (2026-08-18 stats 대조 완료)
- **출고 검사 전 단지 통과.** 교체가능 쌍 0건. 이전 판정은 11곳 중 10곳 반려였다
- hidden 누계 — 남산타운·올선기 20 + 원베일리 정리 20 + 파일럿 외 5곳 72 + 반려 65 = 177편
- 페르소나 39개 등록 완료 (분석 13 + 홍보 22 + 숫자비교표 1 + 팬 듀오 2 + 특이값 1)

편수가 크게 줄어든 건 의도한 결과다. 반려된 글을 남겨 두면 출고 검사 통과가 무의미하다.
다시 채우는 속도는 서모스탯 콜드스타트(단지당 1일 2편)가 상한이고,
특이값 생성기가 라운드당 단지별 1편을 만든다.

**활동 중인 페르소나는 21명이다** (등록 39명). 휴면 21명 중 20명이 홍보 페르소나인데,
파일럿 축소로 그들의 글이 전부 hidden 되면서 통째로 놀고 있다. 살아 있는 홍보 페르소나는
원베일리 2명뿐이다. 그리고 서버에는 있는데 `data/personas.json`에 없는 페르소나가 3명 있다
(보유비용팀·세금메모·장기비용체크). 옛 팩에서 직접 등록된 것들이라 로컬 목록과 서버가 어긋나 있다.

### 알려진 이슈 / 다음 할 일

1. **K-apt 연동 — 완료.** (2026-08-16)
   - 공식 OpenAPI로 **11개 단지 × 2026년 5~6월 = 22건** 확보. 21건 완전, 19건 대조 일치
   - `apis.data.go.kr`은 **차단이 아니었다.** 이전 기록이 틀렸다. 키만 있으면 된다
   - `k-apt.go.kr` 직접 접속은 연결이 끊긴다(Connection reset). OpenAPI가 유일한 공식 경로다
   - 공용관리비: `1613000/AptCmnuseManageCostServiceV2` 항목별 17개 오퍼레이션.
     17개 합산값이 K-apt 공개 총액과 맞는지 매 건 대조한다(`crosscheck`)
   - 키는 `.env`의 `KAPT_SERVICE_KEY`. 활용기간 2026-08-16 ~ 2028-08-16, 오퍼레이션당 일 5,000건
   - 기본정보 서비스도 승인됐다. **관리비부과면적은 `kaptMarea`** 하나만 쓴다.
     `kaptTarea`(건축물대장 연면적)나 `privArea`(전용면적합)를 잘못 집으면 ㎡당 단가가 통째로 틀어진다
   - 개별사용료·장기수선충당금은 아직 [별도 서비스](https://www.data.go.kr/data/15059469/openapi.do)
     미신청이라 공개 페이지 경유다. `breakdown`이 그 값이고, 공용관리비는 공식 조회분이다
   - **주의**: 미공개 월은 빈 item이 와서 그냥 더하면 `0원`이라는 가짜 수치가 된다.
     파크리오 2026-06에서 실제로 났던 사고이고, 지금은 걸러서 `complete: false`로 남긴다
   - **주의**: K-apt가 `kaptdaCnt`(세대수)를 0으로 비워 둔 단지가 있다(디에이치 퍼스티어).
     이때 `hoCnt`로 대체하고 `household_count_source`에 무엇을 썼는지 남긴다
   - **공시가격(브이월드)은 해외 IP 차단이다 — 진단 완료.** (2026-08-18)
     `api.vworld.kr`은 이 환경에서도, 앤트로픽 서버를 경유해도(WebFetch) 똑같이 502다.
     프록시 거부 기록은 0건이고 키와도 무관하다. **한국 IP가 아니면 어떤 환경에서도 안 된다.**

     | 막힘 | 열림 |
     | --- | --- |
     | `api.vworld.kr` · `nsdi.go.kr` · `k-apt.go.kr` · `r-one.co.kr` | `apis.data.go.kr` · `realtyprice.kr` · `reb.or.kr` · `kosis.kr` |

     절차는 `docs/vworld-run.md`. 운영자가 국내 회선에서 한 번 돌려 결과만 커밋하면 된다.
     공시가격은 **가격 자료라 홍보 페르소나가 인용할 수 없다.** 세금·정책 분석 전용이다
   - **`vworld_price.py`의 법정동코드 표가 틀려 있었다.** 손으로 적은 값이라 파일럿 6곳 중
     4곳이 어긋났다(파크리오 자리에 잠실동 코드, 헬리오시티는 아예 없음).
     **틀려도 조회는 성공한다** — 엉뚱한 동의 값이 정상 응답으로 저장된다.
     이제 `scripts/ldcode.py`가 실거래 응답의 `sggCd`+`umdCd`로 확정한다.
     법정동코드 10자리 = 시군구 5 + 읍면동 5다. 손으로 적지 않는다
2. **원베일리 정리 — 게시 완료.** (2026-08-16) 27 → 14편(정리분). 홍보 2인 신설,
   관리비 글 실수치 갱신, 20편 hidden. 팬 듀오 3편이 더해져 최종 노출 17편
3. **숫자비교표 파일럿 — 게시 완료.** 비교 3편(관리비 단가·주차·승강기), verified.
   반응을 보고 주 1~2편 정례화 여부를 정한다
4. **전월세 실거래 — 활용신청 승인.** (2026-08-18) `1613000/RTMSDataSvcAptRent/getRTMSDataSvcAptRent`,
   활용기간 2026-08-18~2028-08-18, 일 10,000건. **키는 기존 `KAPT_SERVICE_KEY`와 같아서 `.env` 변경이 없다**
   - 표본이 매매보다 훨씬 크다. 헬리오시티 3개월 전월세 380건 대 매매 57건
   - **`numOfRows` 1,000 상한에 걸린다.** 강남구 2026-05는 실제 1,901건인데 첫 페이지만 받으면 1,000건이다.
     `rent_volume.py`가 `totalCount`를 보고 끝까지 페이지를 돈다. 이걸 빼면 그 달 앞부분만 보고 비율을 낸다
   - 매매에 없는 필드가 있다 — `contractType`(신규/갱신) 97%, `preDeposit`(갱신 전 조건) 47%,
     `useRRRight`(갱신요구권) 21% 채움. `contractType`과 `preDeposit`은 **채움률이 달라 섞어 쓰면 합이 안 맞는다**
   - 보증금·월세는 `raw_stats`에만 남기고 글에는 비가격 지표만 쓴다(운영자 결정)
5. **팬 듀오 파일럿 — 게시 완료.** 글 5편 + 댓글 루프. 개막전('제일 좋은 동')에
   주민 답이 달리면 후속은 제보자 인용, 없으면 "아직 답이 없다"로 정직하게 쓴다(9번 원칙).
   구경의 위시리스트 시리즈가 다음 소재로 대기
6. **주차 대수 표기 충돌(hidden 처리로 봉합).** `CP1-D007`은 세대당 2.07대라 썼는데
   K-apt 공개값은 1.83대다(5,460 ÷ 2,990). 현재 hidden이라 노출되지 않지만,
   되살릴 경우 기준을 먼저 확인해야 한다. 신규 글들은 전부 1.83대(공식값)를 쓴다
7. **주변 조사 축 — 신설.** (2026-08-18) 4라운드 후보였던 매매 실거래는 **쓸 지표가 하나뿐**이었다.
   해제신고 비율만 갈리고(은마 15.9% 대 리센츠 1.4%) 직거래·법인거래는 전 단지 0~2%다. 6곳을 못 채운다.
   그래서 z-score 비교가 아니라 **주변을 조사해 사실을 제시하고 주민에게 묻는** 축을 열었다(`build_nearby_pack.py`)
   - 소재는 단지마다 다르게 배정한다. 반려 65편의 원인이 "홍보글 7종 템플릿 × 단지"였다
   - 1편은 주변 공공시설(생태보전·도서관·체육·산책로·환승·분수), 2편은 그 단지에만 있는 K-apt 값
     (파크리오 자치관리 · 원베일리 거점장비수거 · 은마 복도식 · 헬리오 주민공동시설 10종)
   - **K-apt 시설 필드는 6곳 중 3곳만 채워져 있다.** 올파포·리센츠는 통째로 비었고
     헬리오는 `관공서() 병원()`처럼 껍데기만 있다. 균등 부활은 불가능하니 있는 곳만 쓴다
   - **robots.txt를 인용 전에 본다.** 강동구청은 AI 봇에 `/web/*/contents/`만 허용하고 게시판(`bbs`)은 막는다.
     생태보전지역 페이지는 허용 경로라 인용했고, 학교 페이지는 차단이라 쓰지 않았다
   - 학교 글은 이번에 못 넣었다. 서울시교육청·강동구청 페이지가 503이라 **출처를 못 읽었다.**
     검색 요약만으로는 쓰지 않는다 — 실제로 잠실환승센터 환승거리가 요약에는 "530m→50m"였는데
     원문은 "390m→120m"였다
8. **생성기 구조.** `build_v3_pack.py`가 `build_top10_pack.py`를 파일 경로로 import한다.
   템플릿·단지 데이터·스케줄러를 모듈로 분리하는 게 맞다
   - **4라운드 소재는 매매 실거래 비가격 지표로 간다**(운영자 결정). 공시가격을 기다리지 않는다.
     후보: 거래 회전율(건수÷세대수) · 해제신고 비율(`cdealType`) · 층별 거래 분포 ·
     면적대 구성 · 중개거래 대 직거래(`dealingGbn`) · 매도자·매수자 구분(`slerGbn`/`buyerGbn`).
     `trade_volume.py`로 이미 조회되고 금액은 건드리지 않는다
9. **템플릿 반복 — 해소.** (2026-08-17)
   출고 검사 첫 실행에서 **11곳 중 10곳 반려**였다(마스킹 후 유사도 1.00 다수). 원인은 구조다 —
   템플릿 t1~t10에 단지를 인자로 넣으면 뼈대가 같을 수밖에 없다.
   "은마 생활 인프라"와 "파크리오 생활 인프라"가 마스킹 후 같은 글이었다.
   `build_outlier_pack.py`가 순서를 뒤집어 **단지별 특이값(z-score)에서 글을 시작**한다.
   지표와 계열(프레임)을 단지마다 겹치지 않게 배정한다.
   반려된 65편은 `--emit-hide`로 뽑아 전부 `hidden` 처리했고, 2라운드 6편을 새로 올렸다.
   **현재 라이브 39편 · 교체가능 0건 · 전 단지 통과.**
   - 라운드마다 지표 풀과 프레임을 통째로 바꾼다. 같은 프레임을 재사용하면 라운드 사이에서 겹친다
   - 1라운드: 규모·설비·인력·비용·시대 (K-apt)
   - 2라운드: 배치·이동·안전·주차·주차방식·청소 (K-apt)
   - 3라운드: 회전·임대차유형·층·권리·평형구성·갱신 (**전월세 실거래**)
   - `validate()`가 이전 라운드 배정을 읽어 같은 단지에 같은 지표·계열이 다시 가지 않게 막는다
   - `python3 scripts/shipping_audit.py --pack <팩>` 으로 **게시 전에** 판정한다. 명세 8.3의 단위는 배치다
   - **3라운드에서 K-apt 축은 사실상 고갈됐다.** 같은 축에서 지표만 더 파면 프레임이 겹친다.
     4라운드를 만들려면 **또 다른 데이터 축**이 필요하다(세금·정책 자료, 공시가격 등)
   - `J()`는 **받침에 따라 갈리는 조사쌍만** 받는다. '에서' 같은 걸 넘기면 두 글자를 쌍으로 오해해
     '파크리오서'가 된다. 실제로 났고, 지금은 `JOSA_PAIRS` 화이트리스트로 막는다
10. **유입이 0이다 — 최우선 과제.** 최근 50편 총 조회 14회·공감 0·사람 글 0건.
   댓글 681개는 전부 AI다. 콘텐츠를 더 만드는 국면이 아니라
   유통(SEO·오프라인)과 참여 전환(무가입 한 줄 답변)을 여는 국면이다. `docs/handoff-2026-08.md` 참조
11. **`/insights/gaps` — 연동 완료.** (2026-08-18) `scripts/gaps.py`가 사이클 시작 리포트를 만든다.
   응답 원본은 `data/gaps/<날짜>.json`에 쌓인다
   - **서버 안내를 그대로 따르면 안 된다.** `guidance`는 "quiet_complexes부터 채워라"인데
     지금 그 15곳은 **우리가 일부러 뺀 단지들**이다. 글을 내렸으니 조용한 게 당연하다.
     그대로 따르면 어제 내린 단지를 오늘 다시 채운다. `gaps.py`가 파일럿과 교집합해서 읽는다
   - 우선순위는 미답변 질문 → 조용한 파일럿 단지 → 참여도 높은데 편수 적은 분야 순이다
   - `PILOT` 목록이 `build_outlier_pack.py`와 중복돼 있다. 한쪽만 바꾸면 어긋난다
12. **주민 답변 0건.** 임장 체크리스트·질문 글에 아직 사람 응답이 없다. 실제 이용자 유입 전까지는
   답이 있는 척하지 않는다. 후속 글은 "아직 답이 모이지 않았다"고 정직하게 쓴다
13. **호가(네이버 등)는 수집하지 않는다 — 법적으로 정리됨.** 네이버 대 다윈중개 사건에서
   1심(2024, 7천만원)에 이어 **2026년 1월 특허법원**이 데이터베이스제작자 권리 침해를 인정하고
   8천만원 배상과 서버 자료 삭제를 명했다. '일시적 복제' 항변은 배척됐다.
   매물 검증에 들인 투자(약 10억원 규모)가 판단 근거였다. 명세 9.6.4와도 어긋난다.
   - 대안은 **전월세 실거래가**다. 매매와 달리 아직 활용신청이 안 돼 있다
     ([data.go.kr/data/15058017](https://www.data.go.kr/data/15058017/openapi.do)). 신청하면 기존 키로 붙는다
   - 매매 실거래는 이미 조회된다(`scripts/trade_volume.py`, 별도 신청 불필요)
14. **K-apt 면적 구간은 틀렸다 — 인용 금지 확정.** (2026-08-18 전월세 실거래로 확인)
   리센츠는 전용면적 기준으로 들어가 있는데(84.99㎡가 60~85 구간) 은마는 4,424세대 중
   4,400세대가 85~135 구간이다. **전월세 실거래로 대조했더니 은마 계약 146건은
   전용 76.79㎡ 89건과 84.43㎡ 57건 두 종뿐이고 85㎡ 초과가 0건이다.** K-apt 구간 쪽이 틀렸다.
   `hh_upto60`·`hh_60_85`·`hh_85_135`·`hh_over135`는 계속 글에 쓰지 않는다.
   대신 **실거래 신고서의 `excluUseAr`은 신뢰할 수 있고**, 3라운드 평형구성 지표가 그걸 쓴다

---

## 7. 작업 흐름

새 콘텐츠 사이클은 이 순서로 돈다.

1. `python3 scripts/gaps.py` — 무엇이 비었는지 먼저 묻는다.
   **`quiet_complexes`를 그대로 따르지 않는다.** 파일럿 밖 단지가 조용한 건 의도한 상태다
2. 조사 — 주간 시황(한국부동산원 발표), 정책, 단지별 뉴스. **출처 URL과 날짜를 반드시 남긴다**
3. `data/complexes.json` 갱신 — 새 사실이 확인되면 반영
4. 생성기 실행 → assert 통과 확인
5. 샘플 2~3건을 눈으로 읽는다. 조사 오류, 중복 문장, 어색한 문단을 여기서 잡는다
6. 게시 → `stats`와 피드로 반영 대조 (글 수·댓글 수·verified 수가 예상과 일치하는지)

5번을 건너뛰면 조사 오류와 인용일 역전 같은 사고가 그대로 나간다. 실제로 두 번 났다.

15. **문말 단조 — 부분 해소.** (2026-08-18) 게시글의 AI 티를 실제로 재 봤다.
   번역투(`~를 통해`) 0건, 피동 남용 0건, "결론적으로/시사하는 바" 0건, "매우/상당히" 0건이다.
   금지 목록과 "사실/해석 분리·질문으로 닫기" 규칙이 이미 문체를 잡고 있다.
   유일하게 걸린 건 **`~니다` 문말이 5~14연속**이라는 점이다.
   - `build_nearby_pack.py` 프레임에 손을 대 최장 14 → 11로 줄였다.
     `build_outlier_pack.py`의 17종은 아직 그대로다(다음 사이클)
   - **산출물이 아니라 생성기 프레임에만 적용한다.** 산출물에 걸면 팩이 재현 불가가 되고,
     같은 윤문 규칙을 6편에 적용하면 문체가 수렴해 **출고 검사 유사도가 오히려 오른다**
   - 그래서 프레임마다 **다른 방식으로** 문말을 깬다. 윤문 뒤 출고 검사를 반드시 다시 돌린다

16. **화면 확인 — 유통 인프라가 갖춰졌다.** (2026-08-21) 8/16 진단에서 최우선 과제였던
   글 상세 404가 해결됐다. `/c/{slug}/p/{id}` SSR, 글별 OG 카드, `sitemap.xml`이 모두 생겼다.
   - 대신 **본문 마크다운은 렌더링되지 않는다.** 우리가 쓴 `**강조**`가 라이브 13곳에서
     별표로 노출되고 있었고, 그중 3곳은 `summary`라 공유 카드 설명에까지 박혔다. 전부 제거하고
     생성기 검증에 금지 항목을 넣었다
   - CTO에게 남길 것: **OG 카드에 큰 숫자 슬롯**이 있으면 캡처 확률이 크게 오른다.
     지금은 제목 텍스트뿐이다. 인제스트에 `key_figure` 같은 필드 하나면 된다
17. **분석 글 계열 신설.** (2026-08-21) `build_analysis_pack.py`.
   홍보·특이값 생성기는 금지 어휘에 '억'·'시세'가 있어 가격을 못 쓴다. 분석 페르소나는 쓸 수 있고,
   대신 **기준을 반드시 붙인다**(명세와 CLAUDE.md 4장).
   - 원베일리 2편 게시 — 평당가 분모 차이, 토지거래허가구역 전후 거래량
   - 담합 유도·매수/매도 지시·전망 단정·서열·우열 비교는 이 팩에서도 막는다
18. **외부 리포트는 대조용으로만 쓴다.** 운영자가 올린 원베일리 심층 리포트를 원자료와 맞춰 봤다.
   130억·105.5억 거래는 정확히 일치했지만 51.2억 거래는 날짜가 틀렸고(5/29 → 실제 5/1),
   "60㎡이하 최초 40억 돌파"는 리포트 자체와 모순이었다. 6/15 122억 거래는 빠져 있었다.
   - **후기 41건은 쓸 수 없다.** 호갱노노·당근·집품·블라인드 출처이고 명세 9.6.3이
     원문·닉네임·URL 저장을 금지한다. 출처 표기를 지운 판본이 와도 마찬가지다
   - 하자 규모("약 600가구")처럼 확인 불가한 수치도 쓰지 않는다. 소송이 걸린 사안이다
