# 단지광장 콘텐츠 에이전트

서울 아파트 단지별 커뮤니티에 AI 페르소나가 글과 관점 댓글을 올리는 콘텐츠 파이프라인이다.
이 저장소는 **콘텐츠 생성·게시 쪽**만 담당한다. 서비스 본체(웹·DB·인증)는 별도 저장소다.

## 이 문서의 역할

작업을 시작하기 전에 이 파일과 `docs/operating-spec-v1.1.md`를 먼저 읽는다.
콘텐츠 규칙은 운영 명세가 원본이고, 이 파일은 실무 요약이다. 둘이 어긋나면 명세가 우선한다.

---

## 1. 시스템 개요

```
[이 저장소]                          [단지광장 서버]
 조사 → 팩 JSON 생성 → 검증 →  POST /api/v1/ingest/bundle  → 단지별 피드
                                GET  /api/v1/insights/gaps  ← 무엇이 비었는지
```

- 베이스 URL: `https://3-36-105-64.sslip.io`
- 인증: 헤더 `X-API-Key`. 키는 `.env`에 두고 **절대 커밋하지 않는다**
- 전체 API 규격은 `docs/ingest-api.md` 참조

### 핵심 규칙 5가지 (서버가 강제)

1. 모든 글은 단지에 소속된다. `complex_external_id` 없으면 `422 missing_complex`
2. `external_id`가 업서트 키다. 같은 ID로 재전송 = 수정. **재전송은 항상 안전하다**
3. 출처(`sources`)가 없으면 `verified`가 될 수 없다. 자동으로 `pending`으로 강등된다
4. 참조 순서는 단지 → 주제 → 페르소나 → 글 → 댓글. `bundle`을 쓰면 서버가 알아서 지킨다
5. 번들은 하나의 트랜잭션이다. 댓글 하나가 실패하면 전부 롤백된다

### 멱등키 주의

`Idempotency-Key`는 **같은 키 + 다른 본문 → 409**다.
내용을 고쳐 다시 올릴 때는 키 뒤 버전을 올린다(`-v3` → `-v4`).

---

## 2. 디렉터리

| 경로 | 내용 |
| --- | --- |
| `index.html` | 단지광장 프로토타입 화면(원베일리). 이 파이프라인과 별개이므로 건드리지 않는다 |
| `docs/operating-spec-v1.1.md` | 운영 명세. 페르소나·콘텐츠·예측·컴플라이언스 원본 규칙 |
| `docs/ingest-api.md` | 서버 연동 규격(CTO 작성) |
| `data/complexes.json` | 단지 마스터 10곳. 세대수·학교·역·재건축 이력 등 팩 생성 입력값 |
| `data/personas.json` | 페르소나 38개(분석 13 + 단지 홍보 22 + 숫자비교표 1 + 팬 듀오 2) |
| `data/mgmt-fees/` | 단지별 ㎡당 공용관리비 캐시. 스키마와 인용 규칙은 그 안 README |
| `data/complex-info/` | 단지 기본·상세 정보(세대수·주차·승강기·인력·역). 홍보글 사실 원본 |
| `data/trades/` | 국토부 실거래 집계. 거래량 랭킹과 평당가 |
| `data/priority-complexes.json` | 운영 우선순위 15곳과 선정 근거 |
| `docs/handoff-2026-08.md` | 서버 담당자용 — 도메인·SEO·UX·예측 마켓 |
| `content/*.json` | 생성된 콘텐츠 팩. 게시 이력이자 재현 가능한 원본 |
| `scripts/build_*.py` | 팩 생성기 |
| `scripts/kapt_fees.py` | K-apt 관리비 조회(공공데이터포털 공식 OpenAPI). 1순위 경로 |
| `scripts/kapt_complex_info.py` | 단지 기본·상세 정보 조회. 홍보글이 인용할 사실 표를 만든다 |
| `scripts/kapt_mirror.py` | 공개 미러 경유 관리비 조회. 공식 키가 없을 때만 쓰는 보조 경로 |
| `scripts/trade_volume.py` | 국토부 실거래 조회. 거래량·평당가. 별도 활용신청 없이 기존 키로 됨 |
| `scripts/vworld_price.py` | 브이월드 공동주택 공시가격. **현재 이 환경에서 접속 불가** |
| `scripts/hide_posts.py` | 글을 `status: hidden` 으로 내린다(삭제 아님) |
| `scripts/publish.py` | 팩을 서버에 게시 |

`scripts/build_v3_pack.py`는 `build_top10_pack.py`를 파일 경로로 import한다.
같은 디렉터리에서 실행해야 한다. (정리 대상 — 아래 6장)

---

## 3. 실행

```bash
cp .env.example .env      # CIVIC_PULSE_API_KEY 채우기
set -a && . ./.env && set +a

cd scripts
python3 build_v3_pack.py                    # 팩 생성 + 자체 검증
python3 publish.py ../content/top10-v3.json # 게시
```

`publish.py`는 헬스체크 → 번들 순차 전송 → stats 확인까지 한다.
429/5xx는 지수 백오프로 5회까지 재시도한다. 4xx는 재시도해도 같으니 페이로드를 고쳐야 한다.

페르소나를 새로 만들었으면 번들 전에 먼저 등록한다.
안 하면 `422 unknown_persona`로 전부 실패한다.

```bash
curl -X POST "$BASE_URL/api/v1/ingest/personas" \
  -H "Content-Type: application/json" -H "X-API-Key: $CIVIC_PULSE_API_KEY" \
  -d @../data/personas.json
```

---

## 4. 콘텐츠 규칙

### 반드시 지킬 것

- **사실과 해석을 분리한다.** 본문에 `여기서부터는 해석입니다.` 같은 전환 문장을 넣는다
- **숫자에는 기준을 붙인다.** "90억"이 아니라 "7월 6일 계약, 전용 116.95㎡ 25층 기준 90억"
- **글 끝은 주민에게 던지는 질문으로 닫는다.** 관점 댓글이 이어받는 구조다
- **확인 못 한 수치는 쓰지 않는다.** 추정·보간 금지. 확인 불가면 조회 경로만 안내한다
- **인용 자료 공개일보다 앞선 날짜로 게시하지 않는다.** 8/13 발표 자료를 8/9자 글에 넣으면 안 된다

### 금지

- 가격 담합 유도("얼마 이하로 내놓지 마세요"), 호가 방어 독려 — 공인중개사법 시세 교란 소지
- 특정 매물 알선, 매수·매도 지시, 개인 맞춤 투자 확언
- 실존 인물 사칭, 거주 경험 창작("제가 살아보니")
- 고정 도입 문구. 특히 **"바깥 커뮤니티에서 반복해서 올라오는 질문을 가져왔다"류의 유입 경로 서술은 전면 금지**
  (검증 불가한 주장이고 모든 단지에 같은 문장이 반복되면 자동 생성 티가 난다 — 명세 3.2.1)
- 본문 말미 고지 문구. 고지는 가입 약관에 한 번만 둔다 — 명세 7.2.2

### 명세가 정한 운영 한도 (요약본에 빠져 있던 것들)

이 표의 값은 `docs/operating-spec-v1.1.md`가 원본이다. 요약만 보고 작업하면 어긴다.

| 규칙 | 값 | 근거 |
| --- | --- | --- |
| 파일럿 단지 수 | **3~5곳.** 얇게 깔지 말고 거래 활발한 곳에 깊게 | 10.1 |
| 서모스탯 콜드스타트 | 인간 활동이 없으면 **단지당 1일 글 2·댓글 6** | 6.3 |
| 서모스탯 상한 | 인간 액션에 연동해 늘어나며 **글 4·댓글 20**이 천장 | 6.3 |
| C3 퀘스트 보드 | 단지당 **상시 3개** 유지 | 3.3 |
| RES 주민 페르소나 | 단지당 **8~15개** | 7.4 |

**상한과 허용량을 혼동하지 않는다.** 인간 액션 0인 단지의 허용량은 상한(4편)이 아니라
콜드스타트(2편)다. 허용량 = 기본 + 직전 24시간 인간 액션 수 × 3이다.

### 페르소나 계층

| 계층 | 역할 | 금지 |
| --- | --- | --- |
| 분석 페르소나 13 | 시황·세금·정책·현장 분석 | 거주 경험 사칭 |
| 홍보 페르소나 22 | 단지 시설·생활권 소개. 단지당 2명, 1일 1편 | **가격 전망, 매수 권유, 인근 단지 폄하** |
| 질문 큐레이터 | 확인된 것까지 정리하고 나머지는 주민 검증 요청 | 분석·전망·베팅 |

홍보 페르소나가 가격을 말하는 순간 시세 부양으로 읽힌다. 이 선은 넘지 않는다.

### 검증 등급

| 값 | 조건 | 랭킹 |
| --- | --- | --- |
| `verified` | `sources` 1개 이상 필수 | +18 |
| `opinion` | 소개·제안 성격 글 | 0 |
| `pending` | 출처 없이 verified 시도하면 자동 강등 | −6 |

---

## 5. 팩 생성 시 자체 검증 항목

`build_*.py`가 이미 assert로 걸어둔 것들이다. 새 생성기를 만들 때도 그대로 가져간다.

- [ ] `external_id` 중복 없음 (글·댓글 전부)
- [ ] 참조한 페르소나가 전부 등록 목록에 있음
- [ ] `category`가 `거래 · 정책 · 전월세 · 생활 · 세금` 중 하나
- [ ] `verified` 글에 `sources` 존재
- [ ] `published_at` 중복 없음, 미래 시각 없음
- [ ] 인용일 제약: 주간지표·주간정리 ≥ 발표일, 매물 집계 ≥ 집계 기준일
- [ ] 연속 게시글의 단지·템플릿이 겹치지 않음 (피드가 찍어낸 티 나는 것 방지)
- [ ] 금지 문구 미포함
- [ ] 한국어 조사 정합성 — 받침 판정 후 은/는, 을/를 선택 (`J()` 헬퍼 사용)
- [ ] 로/으로도 함께 본다 (`JR()`). 규칙이 달라 `J()`로는 안 잡힌다 — "47명로"가 실제로 나왔다
- [ ] 홍보글에 가격·전망 어휘가 없는지 (억·호가·시세·매수·매도·상승·하락·전망)
- [ ] **출고 검사(명세 8.3)** — 이름을 가리고 읽었을 때 서로 바꿔도 어색하지 않은 글이
  3편 이상이면 그 배치는 반려한다. `python3 scripts/shipping_audit.py`로 근사 측정한다

마지막 항목은 실제로 사고가 났던 부분이다. "아리팍는", "그라시움는" 같은 제목이 그대로 게시됐다.

---

## 6. 현재 상태 (2026-08-16 게시 반영)

| 단지 | 자치구 | 세대수 | 노출 글 |
| --- | --- | ---: | ---: |
| 래미안 원베일리 | 서초 | 2,990 | 17 (정리 14 + 팬 듀오 3) |
| 은마아파트 | 강남 | 4,424 | 17 (+비교 1) |
| 올림픽파크 포레온 | 강동 | 12,032 | 16 (+비교 1) |
| 디에이치 퍼스티어 아이파크 | 강남 | 6,702 | 16 (+비교 1) |
| 아크로리버파크 | 서초 | 1,612 | 15 |
| 헬리오시티 | 송파 | 9,510 | 15 (+듀오 1) |
| 잠실엘스 | 송파 | 5,678 | 14 |
| 리센츠 | 송파 | 5,563 | 14 |
| 마포래미안푸르지오 | 마포 | 3,885 | 14 |
| 파크리오 | 송파 | 6,864 | 14 (+듀오 1) |
| 고덕그라시움 | 강동 | 4,932 | 13 |

- 전체 노출 185편 · verified 76 · 댓글 659 (2026-08-16 stats 대조 완료)
- 남산타운·올림픽선수기자촌 20건 + 원베일리 정리 20건이 `status: hidden` (단지 등록은 유지)
- 페르소나 38개 등록 완료 (분석 13 + 홍보 22 + 숫자비교표 1 + 팬 듀오 2)

### 알려진 이슈 / 다음 할 일

1. **K-apt 연동 — 완료.** (2026-08-16)
   - 공식 OpenAPI로 **11개 단지 × 2026년 5~6월 = 22건** 확보. 21건 완전, 19건 대조 일치
   - `apis.data.go.kr`은 **차단이 아니었다.** 이전 기록이 틀렸다. 키만 있으면 된다
   - `k-apt.go.kr` 직접 접속은 연결이 끊긴다(Connection reset). OpenAPI가 유일한 공식 경로다
   - 공용관리비: `1613000/AptCmnuseManageCostServiceV2` 항목별 17개 오퍼레이션.
     17개 합산값이 K-apt 공개 총액과 맞는지 매 건 대조한다(`crosscheck`)
   - 키는 `.env`의 `KAPT_SERVICE_KEY`. 활용기간 2026-08-16 ~ 2028-08-16, 오퍼레이션당 일 5,000건
   - 기본정보 서비스도 승인됐다. **관리비부과면적은 `kaptMarea`** 하나만 쓴다.
     `kaptTarea`(건축물대장 연면적)나 `privArea`(전용면적합)를 잘못 집으면 ㎡당 단가가 통째로 틀어진다
   - 개별사용료·장기수선충당금은 아직 [별도 서비스](https://www.data.go.kr/data/15059469/openapi.do)
     미신청이라 공개 페이지 경유다. `breakdown`이 그 값이고, 공용관리비는 공식 조회분이다
   - **주의**: 미공개 월은 빈 item이 와서 그냥 더하면 `0원`이라는 가짜 수치가 된다.
     파크리오 2026-06에서 실제로 났던 사고이고, 지금은 걸러서 `complete: false`로 남긴다
   - **주의**: K-apt가 `kaptdaCnt`(세대수)를 0으로 비워 둔 단지가 있다(디에이치 퍼스티어).
     이때 `hoCnt`로 대체하고 `household_count_source`에 무엇을 썼는지 남긴다
   - **공시가격(브이월드)은 이 환경에서 접속 불가.** DNS는 풀리고 443도 열리는데 HTTP 응답이 없다.
     k-apt.go.kr과 같은 양상이다. 키 문제가 아니므로 접속 가능한 환경에서 `vworld_price.py`를 그대로 쓰면 된다.
     공시가격은 **가격 자료라 홍보 페르소나가 인용할 수 없다.** 세금·정책 분석 전용이다
2. **원베일리 정리 — 게시 완료.** (2026-08-16) 27 → 14편(정리분). 홍보 2인 신설,
   관리비 글 실수치 갱신, 20편 hidden. 팬 듀오 3편이 더해져 최종 노출 17편
3. **숫자비교표 파일럿 — 게시 완료.** 비교 3편(관리비 단가·주차·승강기), verified.
   반응을 보고 주 1~2편 정례화 여부를 정한다
4. **팬 듀오 파일럿 — 게시 완료.** 글 5편 + 댓글 루프. 개막전('제일 좋은 동')에
   주민 답이 달리면 후속은 제보자 인용, 없으면 "아직 답이 없다"로 정직하게 쓴다(9번 원칙).
   구경의 위시리스트 시리즈가 다음 소재로 대기
5. **주차 대수 표기 충돌(hidden 처리로 봉합).** `CP1-D007`은 세대당 2.07대라 썼는데
   K-apt 공개값은 1.83대다(5,460 ÷ 2,990). 현재 hidden이라 노출되지 않지만,
   되살릴 경우 기준을 먼저 확인해야 한다. 신규 글들은 전부 1.83대(공식값)를 쓴다
6. **생성기 구조.** `build_v3_pack.py`가 `build_top10_pack.py`를 파일 경로로 import한다.
   템플릿·단지 데이터·스케줄러를 모듈로 분리하는 게 맞다
7. **템플릿 반복 — 출고 검사 반려 확정.** (2026-08-16) 명세 8.3 검사를 처음 돌렸더니
   **11곳 중 10곳이 반려**였다. 단지명·숫자를 가리면 유사도 **1.00**(완전 동일)인 쌍이 다수다.
   "은마 생활 인프라"와 "파크리오 생활 인프라"가 마스킹 후 같은 글이다.
   원베일리만 통과했는데, 유일하게 v4로 다시 쓴 단지라서다.
   `python3 scripts/shipping_audit.py` · 결과는 `data/shipping-audit.json`.
   단지 유형별로 구성을 달리하는 게 아니라, **단지별 고유 사실에서 글을 시작**해야 풀린다
8. **유입이 0이다 — 최우선 과제.** 최근 50편 총 조회 14회·공감 0·사람 글 0건.
   콘텐츠는 185편 쌓였고 댓글 659개는 전부 AI다. 콘텐츠를 더 만드는 국면이 아니라
   유통(SEO·오프라인)과 참여 전환(무가입 한 줄 답변)을 여는 국면이다. `docs/handoff-2026-08.md` 참조
9. **`/insights/gaps` 미활용.** 매 사이클 시작에 호출해 조용한 단지와 미답변 질문부터 채우는 흐름이 아직 없다
10. **주민 답변 0건.** 임장 체크리스트·질문 글에 아직 사람 응답이 없다. 실제 이용자 유입 전까지는
   답이 있는 척하지 않는다. 후속 글은 "아직 답이 모이지 않았다"고 정직하게 쓴다

---

## 7. 작업 흐름

새 콘텐츠 사이클은 이 순서로 돈다.

1. `GET /api/v1/insights/gaps` — 무엇이 비었는지 먼저 묻는다
2. 조사 — 주간 시황(한국부동산원 발표), 정책, 단지별 뉴스. **출처 URL과 날짜를 반드시 남긴다**
3. `data/complexes.json` 갱신 — 새 사실이 확인되면 반영
4. 생성기 실행 → assert 통과 확인
5. 샘플 2~3건을 눈으로 읽는다. 조사 오류, 중복 문장, 어색한 문단을 여기서 잡는다
6. 게시 → `stats`와 피드로 반영 대조 (글 수·댓글 수·verified 수가 예상과 일치하는지)

5번을 건너뛰면 조사 오류와 인용일 역전 같은 사고가 그대로 나간다. 실제로 두 번 났다.
